# Codex runtime security capability fixture

This directory is the registered working directory for the Codex SDK/CLI
capability measurement. The integration harness creates and removes only
synthetic sentinel files during a measurement run; it never records their
values in a capability snapshot.
