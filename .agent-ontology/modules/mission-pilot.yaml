{
  "version": 1,
  "id": "mission-pilot",
  "label": "Mission Pilot",
  "summary": "Workspace package that owns the long-lived Mission Pilot session, delegated-user operation loop, persistence, and recovery.",
  "ubiquitousLanguage": [{ "name": "PilotSession", "meaning": "The durable automation controller associated one-to-one with a Task." }],
  "responsibilities": ["Advance authorized Pilot phases.", "Persist Pilot events and recovery state.", "Use injected host ports for user-equivalent public operations."],
  "ownedPaths": ["packages/mission-pilot/**"],
  "readMostlyPaths": ["api/composition/mission-pilot/**", "src/composition/mission-pilot/**"],
  "ownedData": { "tables": ["mission_pilot_sessions", "mission_pilot_events"], "files": [] },
  "invariants": [{ "id": "pilot-authorization-gate", "statement": "Pilot automation advances only while the session is playing and authorized.", "check": "Mission Pilot contract and recovery tests cover the gate." }],
  "allowedMutations": ["Pilot phase progression", "Pilot recovery", "Pilot archive transition"],
  "forbiddenMutations": ["Mission decomposition internals", "Queue persistence internals", "Review verdict internals", "Repository, filesystem, Git, or Coding Agent internals"],
  "allowedCrossModule": [],
  "verification": { "baseline": [{ "command": "bun test tests/mission-pilot-contract.test.ts", "expect": "Mission Pilot contracts pass." }], "focused": [{ "command": "bun test tests/mission-pilot-plan-coordinator.test.ts tests/mission-pilot-post-queue-state.test.ts tests/mission-pilot-closeout.test.ts", "expect": "Pilot phases pass." }], "full": [{ "command": "bun run verify", "expect": "Repository verification passes." }] },
  "completionReport": { "require": ["primaryModule", "boundaryCrossings", "invariantsChecked", "verification"] }
}
