text = '''最終 outcome は変更せず、rubric と evidence に基づく findings / humanCallouts / follow-ups だけを返してください。

ReviewResult は直接返さず、ReviewerDraft JSON だけを返してください。

Rubric: [[rubricTitle]] ([[rubricId]])

Hints:
[[hints]]

EvidencePack:
[[evidenceJson]]'''

[variables.evidenceJson]
profile = "trusted.inline"

[variables.hints]
profile = "trusted.inline"

[variables.rubricId]
profile = "trusted.inline"

[variables.rubricTitle]
profile = "trusted.inline"
