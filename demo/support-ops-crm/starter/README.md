# Support Ops CRM starter

This disposable repository is intentionally incomplete. The deterministic demo
provider implements `src/tickets.mjs`, runs the test suite, and records the diff
and command output as review evidence.
